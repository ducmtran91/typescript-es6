var forOfs = ["immediately", "capability", "enforce", "invoke", "conversely"];
for(let index in forOfs) {
    console.log(forOfs[index]);
}

for(let item of forOfs) {
    console.log(item);
}

var forName = "variety";
for(let c of forName) {
    console.log(c);
}
