"use strict";
var destructuring = {
    immediately: 'đồng thời',
    capability: 'có khả năng',
    hoisting: 'nâng lên, cẩu, tời nâng',
    conversely: 'ngược lại',
    separate: 'bóc, tách',
    invoke: 'gọi',
    variety: 'đa dạng'
};
var immediately = destructuring.immediately, cap = destructuring.capability, hoisting = destructuring.hoisting, conversely = destructuring.conversely, separate = destructuring.separate, invoke = destructuring.invoke, variety = destructuring.variety;
console.log(cap);
