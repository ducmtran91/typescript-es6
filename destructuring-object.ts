var destructuring = {
    immediately: 'đồng thời',
    capability: 'có khả năng',
    hoisting: 'nâng lên, cẩu, tời nâng',
    conversely: 'ngược lại',
    separate: 'bóc, tách',
    invoke: 'gọi',
    variety: 'đa dạng'
}

let {immediately, capability : cap, hoisting, conversely, separate, invoke, variety} = destructuring;

console.log(cap);